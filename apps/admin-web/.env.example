# Local UI planning mode only. Production builds always fail closed until auth is wired.
VITE_ADMIN_PREVIEW_MODE=true

# Defaults to the Vite /api proxy when unset.
# VITE_API_BASE_URL=http://127.0.0.1:8000
